class Car {
  final String name;
  final String asset;
  final int places;
  final bool isElectric;

  Car({
    required this.name,
    required this.asset,
    required this.places,
    required this.isElectric,
  });
}